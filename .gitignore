data/*
!data/sample/
__pycache__/
.venv/
report/
*.pyc

# scratch output from running the tools (datasets, debug overlays, --json dumps)
demo*/
hard*/
overlay*.png
out.json
result.json

# macOS metadata. The AppleDouble "._" sidecars appear for every file when the
# repo lives on a non-HFS volume (exFAT, e.g. an external SanDisk), so they turn
# up in bulk rather than one at a time.
._*
.DS_Store
